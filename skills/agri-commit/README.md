# agri-commit

依 Conventional Commits 1.0.0 規範暫存變更並建立 Git commit。

## 安裝

```bash
npx -y skills@latest add BeehiveDT/skill-forge -g -a claude-code -a codex -s agri-commit
```

## 使用方式

```text
/agri-commit
/agri-commit 描述變更的原因或上下文
```
